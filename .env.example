THROTTLE_ENV=dev
THROTTLE_API_KEYS=dev_key_123,dev_key_456
THROTTLE_REQUIRE_API_KEY=true
THROTTLE_REDIS_URL=redis://localhost:6379/0

# Set to 3 for demos — makes it easy to trigger 429s in a few requests.
# Increase for normal use.
THROTTLE_RATE_LIMIT=3
THROTTLE_WINDOW_SECONDS=60
